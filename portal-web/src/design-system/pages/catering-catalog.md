# Catering & Catalog pages

Use the same list-page shell as Trolley List:

1. `PageHeader` (badge / title / description / actions)
2. `FilterBar` (search + filters)
3. Table inside `data-table-wrap--ops`
4. Optional `page-shell__footer` for counts / publish actions

Do **not** introduce OpsToolbar / sky-accent context bars.
